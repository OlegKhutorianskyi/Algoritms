Для первого метода сложность составляет O(log(a+b)), поскольку цикл while будет выполняться до тех пор, пока a и b не станут равными, и на каждой итерации разница между a и b уменьшается как минимум на 1. Поскольку значение a+b всегда будет постоянным, количество итераций будет пропорционально логарифму (a+b).

Для второго метода сложность составляет O(n^3), так как имеется три вложенных цикла, каждый из которых имеет сложность O(n).

Для третьего метода сложность равна O(n^2), так как внешний цикл имеет сложность O(n), а внутренний цикл имеет сложность O(n/i), которая будет O(n) когда я постоянен.
